export declare enum TokenRestrictionFlag {
    /**
     * Allow only incoming transactions containing a a given token identifier.
     */
    AllowToken = 2,
    /**
     * Block incoming transactions containing a given token identifier.
     */
    BlockToken = 32770
}
